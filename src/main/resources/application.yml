server:
  port: 8085

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL:}
    username: ${DB_USERNAME:}
    password: ${DB_PW:}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  graphql:
    path: /spotemo
    graphiql:
      enabled: true
